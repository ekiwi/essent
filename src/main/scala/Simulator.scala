package essent

//import java.nio.file.Files
//import java.nio.file.Paths
//import net.openhft.compiler.CompilerUtils

class Simulator(firFilePath : String = null) {
  //Driver.main(Array("-O0", "-java", firFilePath))
  //val javaFilePath = firFilePath.replaceAll(".fir", ".java")
  //val javaFileContent = Files.readString(Paths.get(javaFilePath))
  //val className = firFilePath.split("/").last.split("\\.")(0)
  //val aClass = CompilerUtils.CACHED_COMPILER.loadFromJava(className, javaFilePath)
  //val runner: Runnable = aClass.asInstanceOf[Runnable]
  //runner.run()
}
